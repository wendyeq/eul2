# eul2

菜单栏系统监视器。部署目标 macOS 12+。

## 系统版本

产品功能在部署目标到当前系统上同一套：菜单内容、居中弹出、钉住、齿轮/退出、额度，不按系统版本开关。

`#available` / `#unavailable` 只包旧系统上不存在的符号或系统入口（例如 27 的 `NSStatusItem` expanded session、26 的 `glassEffect`）。宿主 API 可以分叉；分叉之上共用同一套 UI 与定位。数据源有无按能力分支（例如蓝牙 HID），不按系统版本分支。

每处新增版本判断须能指出缺了哪个符号或哪个系统入口。布局、宽度、文案、对齐不绑系统版本。
